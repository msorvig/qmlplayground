import QtQuick

Rectangle {
    anchors.fill: parent
    color: palette.window

    Text {
        anchors.centerIn: parent
        text: "Hello, QML!"
        color: palette.windowText
        font.pixelSize: 32
    }
}
